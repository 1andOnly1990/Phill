package com.phillips.phill.di

/**
 * RepositoryModule is intentionally empty.
 * All repositories use @Inject constructor with @Singleton scope,
 * so Hilt auto-provides them without explicit bindings.
 *
 * This file is retained as a placeholder in case interface-based
 * repository abstractions are needed for testing in the future.
 */
